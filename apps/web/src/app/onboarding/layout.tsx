export const metadata = {
  title: 'Onboarding – AlphaVital',
};

import './sleep-fix.css';

export default function OnboardingLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return <>{children}</>;
}
